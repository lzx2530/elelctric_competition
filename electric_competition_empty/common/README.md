# common

这一层放跨模块复用的最小公共组件，不放任何业务语义。

当前内容：
- `types.h`：基础类型别名
- `status.h`：统一状态码
- `math_util.h`：常用限幅、符号、角度包络函数
- `ringbuf.*`：中断收包用环形缓冲区
- `soft_timer.*`：主循环软定时器

约束：
- 不直接依赖具体外设
- 不依赖应用层和驱动层
- 只保留足够小、足够稳定的通用代码
