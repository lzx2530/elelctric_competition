# bsp

这一层是 `SysConfig/DriverLib` 的收口层，负责把 MSPM0 外设访问包装成稳定接口。

当前内容：
- `bsp_gpio.*`：方向脚、激光、蜂鸣器、LED、灰度输入、编码器引脚读取
- `bsp_pwm.*`：直流电机 PWM 和步进 STEP 频率设置
- `bsp_uart.*`：调试串口发送、K230 接收环形缓冲
- `bsp_i2c.*`：I2C 阻塞式寄存器读写

约束：
- `DL_*` 调用尽量集中在这一层
- 只描述硬件能力，不写底盘/云台/显示等业务逻辑
- 后续换 MCU 时，优先改这一层
