# algo

这一层放不依赖具体硬件的控制与数据处理算法。

当前内容：
- `algo_pid.*`：位置式、增量式、串级 PID
- `algo_filter.*`：低通、中值、滑均、限幅、斜坡
- `algo_fusion.*`：六轴姿态融合起步版

约束：
- 不直接访问 GPIO、UART、I2C、Timer
- 参数通过结构体传入，不走宏定义调参
- 优先保持小而明确，便于 VOFA+ 联调
